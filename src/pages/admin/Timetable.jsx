import TimetableManager from '../../components/TimetableManager.jsx'

// Admin Timetable Overview — upload / replace / view a timetable file per class.
export default function AdminTimetable() {
  return <TimetableManager title="Timetable Overview" />
}
