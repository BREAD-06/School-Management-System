import TimetableManager from '../../components/TimetableManager.jsx'

// Teacher Timetable — upload / replace / view a timetable file per class.
export default function TeacherTimetable() {
  return <TimetableManager title="Timetable" />
}
